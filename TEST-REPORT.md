# รายงานผลการทดสอบ LeaveEasy (US-01 ถึง US-09)

> ทดสอบแบบ black-box ด้วย Playwright MCP บนเบราว์เซอร์จริงที่ http://localhost:3000 ต่อ Firebase project `leaveeasy-aa5a3`
> ขอบเขต: เฉพาะ US-01 ถึง US-09 (สัปดาห์ 6-8) ตามที่ระบุ — ไม่ทดสอบ US-10/11/12
> ทดสอบครบทั้ง 3 บทบาท (employee / manager / hr) รวมถึงกรณีไม่ได้ล็อกอิน โดยใช้บัญชีทดสอบที่สมัครใหม่ผ่าน `signup.html` แล้วปรับ `role` ผ่าน Firebase Console สำหรับ manager และ hr

## สรุปรวม

ทดสอบครบทั้ง 9 US ในขอบเขต และครบทุกเกณฑ์การยอมรับ (acceptance criteria) ที่ระบุไว้ในสเปกหัวข้อ 3

- **ผ่านทั้งหมด**: US-01, US-02, US-03, US-04, US-05, US-06, US-07, US-08, US-09 (9/9)
- ไม่พบบั๊กที่ทำให้ฟีเจอร์ใช้งานไม่ได้ตามสเปก
- พบเพียงข้อสังเกตเล็กน้อย 1 จุด (ไม่ใช่บั๊กตามเกณฑ์การยอมรับ) — ดูท้าย US-06
- เกณฑ์ด้านความปลอดภัย/สิทธิ์ทุกจุดถูกทดสอบจริงทั้งจากพฤติกรรม UI และจากการยิงคำสั่งอ่าน/เขียน/ลบ Firestore ตรง ๆ ผ่าน browser console (ไม่ใช่แค่ดูว่าปุ่มถูกซ่อน) เพื่อยืนยันว่า Security Rules บังคับจริงในระดับเซิร์ฟเวอร์

---

### US-01 — ดูรายการใบลาทั้งหมด
**สถานะ: ผ่าน**

ขั้นตอนที่ทำจริงและผลลัพธ์:
- ผู้ใช้ employee ที่ยังไม่มีใบลาเลย เห็นข้อความ "ยังไม่มีใบขอลาในระบบ" (ไม่ใช่ตารางว่าง) — ผ่าน
- ตารางแสดงคอลัมน์ครบ: หัวข้อ · ประเภทการลา · สถานะ · ผู้ขอลา · วันที่ลา — ผ่าน
- ชื่อผู้ขอลาแสดงเป็นภาษาไทยเสมอ (เช่น "สมชาย ใจดี", "ทดสอบ พนักงานหนึ่ง") ไม่มีรหัสอย่าง `u001` หลุดมาที่หน้าจอ — ผ่าน
- ป้ายสถานะสีถูกต้องครบทั้ง 3 ค่า: รอพิจารณา = เหลือง, อนุมัติ = เขียว, ไม่อนุมัติ = แดง (ตรวจด้วยภาพหน้าจอจริงตอนล็อกอินเป็น manager ที่เห็นข้อมูลตัวอย่างครบทั้ง 3 สถานะพร้อมกัน) — ผ่าน
- กดที่แถวไปหน้ารายละเอียดของใบนั้นได้ถูกต้อง — ผ่าน
- **employee เห็นเฉพาะใบของตัวเอง**: ทดสอบด้วย 2 บัญชี employee แยกกัน บัญชีที่ไม่มีใบลาของตัวเองไม่เห็นใบของอีกบัญชีเลยแม้มีอยู่จริงในระบบ — ผ่าน
- **manager/hr เห็นทุกใบ**: ล็อกอินเป็น manager และ hr แล้วเปิดหน้ารายการ ทั้งสองบัญชีเห็นครบทั้ง 7 ใบ (5 ใบตัวอย่างตามสเปก + 2 ใบทดสอบที่สร้างขึ้นระหว่างทดสอบ) พร้อมชื่อผู้ขอลาที่ต่างกันครบ (สมชาย/สมศรี/บัญชีทดสอบ) — ผ่าน

---

### US-02 — ยื่นใบขอลาใหม่
**สถานะ: ผ่าน**

- ฟอร์มมีครบ: หัวข้อ · เหตุผลการลา (textarea) · ประเภทการลา (dropdown) · วันที่เริ่มลา · วันที่สิ้นสุด · ปุ่มบันทึก/ยกเลิก — ผ่าน
- dropdown ประเภทการลาอ่านจาก collection `leaveTypes` จริง (ทดสอบเพิ่มประเภทใหม่ผ่านหน้า hr แล้วเห็นโผล่ในฟอร์มนี้ทันที — ดู US-06) — ผ่าน
- กดบันทึกทั้งที่ไม่กรอกอะไรเลย ขึ้นคำเตือน "กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก" ไม่บันทึก — ผ่าน
- กรอกวันที่สิ้นสุดมาก่อนวันที่เริ่มลา ขึ้นคำเตือน "วันที่สิ้นสุดต้องไม่มาก่อนวันที่เริ่มลา" ไม่บันทึก และค่าที่กรอกไว้ก่อนหน้ายังอยู่ครบ (ไม่หายเวลาเจอ error) — ผ่าน
- กรอกครบและถูกต้อง กดบันทึกสำเร็จ → มีใบลาใหม่จริงใน Firestore, เด้งกลับหน้ารายการ — ผ่าน
- ใบใหม่มีสถานะเริ่มต้น "รอพิจารณา" อัตโนมัติเสมอ (ยืนยันจากทั้งฝั่ง client และจาก Security Rules ที่บังคับ `status == 'รอพิจารณา'` ตอน create) และมีวันเวลาที่ยื่นติดมาอัตโนมัติ — ผ่าน
- ปุ่ม "ยกเลิก" เป็นลิงก์กลับหน้ารายการโดยไม่บันทึกข้อมูล — ผ่าน

---

### US-03 — เปิดดูรายละเอียดใบลา
**สถานะ: ผ่าน**

- แสดงครบทุกช่อง: หัวข้อ · เหตุผล · ประเภทการลา · วันที่ลา (เริ่ม-สิ้นสุด) · ผู้ขอลา · ผู้อนุมัติ · สถานะ · วันที่ยื่น — ผ่าน
- ใบที่ยังไม่มีผู้อนุมัติ (approverId ว่าง) แสดง "ยังไม่ได้กำหนดผู้อนุมัติ" ถูกต้อง — ผ่าน
- **ความเห็นเรียงเก่า→ใหม่**: ทดสอบเขียนความเห็น 2 รายการจาก 2 บัญชี (manager แล้วตามด้วย hr) ในใบเดียวกัน ผลลัพธ์แสดงความเห็นของ manager (เวลาก่อน) อยู่บนสุด ตามด้วยความเห็นของ hr (เวลาหลัง) เรียงถูกต้องจากเก่าไปใหม่ — ผ่าน
- มีปุ่ม "← กลับไปหน้ารายการ" ใช้งานได้ — ผ่าน

---

### US-04 — เปลี่ยนสถานะใบลา
**สถานะ: ผ่าน**

- ในหน้ารายละเอียดของใบลาตัวเอง (สถานะ รอพิจารณา) บัญชี employee **ไม่เห็น** ปุ่มอนุมัติ/ไม่อนุมัติเลย — ผ่าน
- ในหน้ารายละเอียดใบลาของคนอื่น บัญชี manager/hr **เห็น** ปุ่มอนุมัติ/ไม่อนุมัติ (เฉพาะตอนสถานะยัง รอพิจารณา) — ผ่าน
- กดปุ่ม "อนุมัติ" (บัญชี manager) → สถานะเปลี่ยนเป็น "อนุมัติ" ทันทีบนหน้าจอ และค่าบันทึกจริงใน Firestore ทุกช่องอื่น (หัวข้อ/เหตุผล/ประเภท/วันที่/ผู้ขอลา) ไม่เปลี่ยนแปลง มีเฉพาะ `status` ที่เปลี่ยน — ผ่าน
- กดปุ่ม "ไม่อนุมัติ" **โดยยังไม่มีความเห็นมาก่อน** (บัญชี manager, ใบของ employee2) → ระบบขึ้น alert บล็อกทันที "ต้องเขียนความเห็นอย่างน้อย 1 รายการก่อน จึงจะกดไม่อนุมัติได้" สถานะไม่เปลี่ยน (ยังเป็นรอพิจารณา) — ผ่าน
- เขียนความเห็นก่อนแล้วกด "ไม่อนุมัติ" อีกครั้ง → สำเร็จ สถานะเปลี่ยนเป็น "ไม่อนุมัติ" — ผ่าน
- ใบที่ตัดสินแล้ว (อนุมัติ/ไม่อนุมัติ) ปุ่มอนุมัติ/ไม่อนุมัติหายไป แสดงข้อความ "ใบนี้พิจารณาแล้ว จึงเปลี่ยนสถานะต่อไม่ได้" แทน เปลี่ยนสถานะต่อไม่ได้จริง — ผ่าน
- **ทดสอบเจาะเพิ่ม (defense-in-depth)**: ยิง `updateDoc` เปลี่ยน `status` ตรง ๆ ผ่าน Firestore console 3 กรณี — (1) employee พยายามเปลี่ยนสถานะใบตัวเอง (2) manager พยายามเปลี่ยนสถานะใบที่อนุมัติไปแล้วกลับเป็นค่าอื่น — ทั้งสองกรณีได้ `permission-denied` ยืนยันว่า Security Rules บังคับจริง ไม่ใช่แค่ซ่อนปุ่มฝั่ง UI — ผ่าน

---

### US-05 — เขียนความเห็นการอนุมัติ
**สถานะ: ผ่าน**

- กดส่งความเห็นโดยไม่พิมพ์อะไร ขึ้นคำเตือน "พิมพ์ข้อความก่อน จึงจะส่งความเห็นได้" ไม่บันทึก — ผ่าน
- พิมพ์ข้อความแล้วส่ง → บันทึกจริงใน subcollection `leaveRequests/{id}/approvals` (ยืนยันด้วยการยิง `addDoc` ตรงเข้า path `leaveRequests/{id}/approvals` และการลบผ่าน `firebase firestore:delete -r` ที่พบเอกสารย่อยจริงในนั้น) แสดงชื่อผู้เขียน ข้อความ และเวลาให้ถูกต้องทันที — ผ่าน
- ทดสอบทั้งฝั่ง employee เขียนในใบตัวเอง และฝั่ง manager/hr เขียนในใบของคนอื่น — ทำงานถูกต้องทั้งคู่ — ผ่าน
- ทดสอบเจาะเพิ่ม: employee อีกคน (ไม่ใช่เจ้าของใบ ไม่ใช่ manager/hr) ยิง `addDoc` เข้า `approvals` ของใบคนอื่นตรง ๆ → `permission-denied` ยืนยันว่ากฎเฝ้าข้อมูลครอบ subcollection จริง — ผ่าน

---

### US-06 — จัดการประเภทการลา
**สถานะ: ผ่าน**

- **hr**: เข้า `leave-types.html` ได้ เห็นตารางประเภทการลาทั้งหมด (ลาพักร้อน/ลาป่วย/ลากิจ) พร้อมช่องเพิ่มใหม่และปุ่มแก้ไข/ลบต่อแถว — ผ่าน
- เพิ่มประเภทใหม่ ("ลาทดสอบระบบ") → โผล่ในตารางทันที และโผล่ใน dropdown ของหน้า `new-leave-request.html` ทันทีโดยไม่ต้องรีเฟรช — ผ่าน
- แก้ไขชื่อประเภท (ผ่านกล่อง prompt) → อัปเดตในตารางทันที — ผ่าน
- ลบประเภท → หายจากตารางทันที (ทดสอบและลบข้อมูลทดสอบออกเรียบร้อยแล้ว เหลือแค่ 3 ประเภทเดิมตามสเปก) — ผ่าน
- **employee เข้า `leave-types.html` ตรงผ่าน URL ถูกเด้งออก** ไปหน้า `index.html` ทันที ไม่เห็นเนื้อหาเลย — ผ่าน
- **manager เข้า `leave-types.html` ตรงผ่าน URL ก็ถูกเด้งออกเช่นกัน** (ไม่ใช่แค่ employee) — ผ่าน
- ทดสอบเจาะเพิ่ม: บัญชี manager ยิง `addDoc` เข้า collection `leaveTypes` ตรง ๆ → `permission-denied` ยืนยันว่ามีเฉพาะ hr เท่านั้นที่แก้ไขได้จริงในระดับ Security Rules — ผ่าน

ข้อสังเกตเล็กน้อย (ไม่ใช่บั๊กตามเกณฑ์การยอมรับ): ลิงก์ "จัดการประเภทการลา" ที่แสดงอยู่ในตัวเนื้อหาหน้า `index.html` (ส่วนหัวข้อ "หน้าจอของระบบ") ยังคงปรากฏให้ employee/manager เห็นและกดได้ แม้กดแล้วจะถูกเด้งออกจาก `leave-types.html` ทันทีอยู่ดี (ระบบนำทาง/แถบเมนูบนสุด (`nav.js`) ทำถูกต้องอยู่แล้ว คือซ่อนลิงก์นี้ออกจาก navbar สำหรับ employee/manager และแสดงเฉพาะ hr — จุดที่สังเกตคือเฉพาะลิงก์ในตัวเนื้อหาหน้า index เท่านั้นที่ยังไม่ได้กรองตามบทบาท)

---

### US-07 — ลบใบลาของตัวเอง
**สถานะ: ผ่าน**

- ปุ่ม "ลบใบลานี้" ปรากฏเฉพาะในหน้ารายละเอียดใบลาของตัวเองที่สถานะยัง "รอพิจารณา" เท่านั้น — ผ่าน
- กดลบขึ้นกล่องยืนยัน "ยืนยันลบใบขอลานี้หรือไม่? การลบไม่สามารถย้อนกลับได้" ทุกครั้ง ไม่มีทางลบได้โดยไม่ผ่านการยืนยัน — ผ่าน
- กด "ยกเลิก" ในกล่องยืนยัน → ใบลายังอยู่ครบ ไม่มีอะไรหาย — ผ่าน
- กด "ยืนยัน" → ใบลาถูกลบออกจาก Firestore จริง และหน้าจอเด้งกลับไปหน้ารายการ — ผ่าน
- **ใบที่สถานะอื่นนอกจากรอพิจารณา ปุ่มลบต้องกดไม่ได้**: ทดสอบกับใบของ employee1 เองที่เพิ่งถูก manager กดอนุมัติ → เปิดหน้ารายละเอียดแล้ว **ไม่มีปุ่มลบปรากฏเลย** แม้เป็นเจ้าของใบเอง — ผ่าน
- **คนอื่นลบใบที่ไม่ใช่ของตัวเองไม่ได้ แม้เป็น manager/hr**: ทดสอบเจาะด้วยการยิง `deleteDoc` ตรง ๆ 3 กรณี — (1) employee อีกคนลบใบของ employee1 (2) manager ลบใบของ employee2 (3) hr ลบใบของ employee2 — **ทั้ง 3 กรณีได้ `permission-denied` เหมือนกันหมด** ยืนยันว่าไม่มีบทบาทไหนลบใบของคนอื่นได้เลยจริง ตรงตามตารางสิทธิ์ในสเปกหัวข้อ 2 — ผ่าน
- ทดสอบเจาะเพิ่ม: เจ้าของใบเองพยายามลบใบของตัวเองที่ถูกอนุมัติไปแล้ว (สถานะไม่ใช่รอพิจารณา) ผ่าน `deleteDoc` ตรง ๆ → `permission-denied` เช่นกัน (กฎ "ลบได้เฉพาะตอนรอพิจารณา" บังคับจริงแม้เป็นเจ้าของ) — ผ่าน

---

### US-08 — ล็อกอินและเห็นเฉพาะใบลาของตัวเอง
**สถานะ: ผ่าน**

- สมัครสมาชิกสำเร็จ → มีเอกสารใหม่ใน `users/{uid}` พร้อม `role: "employee"` เสมอ (ยืนยันจากพฤติกรรม: สมัครใหม่ 4 บัญชี ทุกบัญชีเริ่มต้นเป็น employee ทำสิ่งที่ employee ทำได้เท่านั้น จนกว่าจะไปปรับ role ผ่าน Console ด้วยมือ — สอดคล้องกับกฎ Security Rules ที่บังคับ `role == 'employee'` ตอน create เอกสารผู้ใช้เสมอ ป้องกันคนสมัครแล้วตั้งตัวเองเป็น manager/hr) — ผ่าน
- ใบลาที่สร้างใหม่มี `requesterId`/`requesterName` เป็นของบัญชีที่ล็อกอินอยู่จริงเสมอ (ตรวจจากหน้ารายละเอียดของทุกใบที่สร้างระหว่างทดสอบ และจาก Security Rules ที่บังคับ `requesterId == request.auth.uid` ตอน create) — ผ่าน
- ล็อกอิน/ออกจากระบบทำงานถูกต้อง (ออกจากระบบเด้งไป `login.html` ทันที, ล็อกอินใหม่ด้วยอีเมล/รหัสผ่านเดิมสำเร็จ) ทดสอบซ้ำกับทั้ง 4 บัญชี — ผ่าน
- **คนที่ไม่ได้ล็อกอิน อ่านข้อมูลไม่ได้เลย**: เปิดหน้า `leave-requests.html`, `new-leave-request.html`, `leave-request-detail.html?id=...`, `leave-types.html` ตรง ๆ ทุกหน้าโดยไม่ล็อกอิน ถูกเด้งไป `login.html` ทั้งหมด ไม่มีหน้าไหนหลุดให้เห็นเนื้อหา — ผ่าน
- **ผู้ขอลาคนหนึ่งเปิดใบลาของอีกคนไม่ได้**: เปิด URL รายละเอียดใบลาของอีกบัญชีตรง ๆ → ขึ้น "ไม่พบใบขอลาที่ต้องการ" (เกิดจาก Security Rules ปฏิเสธการอ่าน ไม่ใช่ใบถูกลบจริง — ยืนยันด้วย error `permission-denied` จากการยิง query ตรง) — ผ่าน
- **ผู้อนุมัติ (manager) และฝ่ายบุคคล (hr) เปิดใบลาของใครก็ได้ทุกใบ**: ทดสอบให้ manager และ hr เปิด URL รายละเอียดใบลาของ employee2 ตรง ๆ ทั้งคู่เปิดเห็นข้อมูลครบถูกต้อง (คนละใบกับที่ตัวเองเป็นเจ้าของ) — ผ่าน
- **กฎเฝ้าข้อมูลครอบ subcollection `approvals` ด้วย**: ทดสอบ employee อีกคนเขียนความเห็นในใบของคนอื่นตรง ๆ ผ่าน `addDoc` → `permission-denied` (ดูรายละเอียดใน US-05) — ผ่าน

---

### US-09 — ปุ่มให้ AI จัดประเภทการลาอัตโนมัติ
**สถานะ: ผ่าน**

- เหตุผล "มีไข้และปวดหัวมาก ต้องพักผ่อนที่บ้าน" → AI เลือก "ลาป่วย" ถูกต้อง — ผ่าน
- เหตุผล "ลูกป่วยต้องพาไปหาหมอที่โรงพยาบาล" → AI เลือก "ลาป่วย" (สมเหตุสมผล)
- เหตุผล "ลาไปเที่ยวพักผ่อนกับครอบครัวที่ทะเล" → AI เลือก "ลาพักร้อน" ถูกต้อง — ผ่าน
- ทุกครั้งที่จัดประเภทสำเร็จ มีป้ายกำกับ "🤖 ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน" ปรากฏตรงตามข้อความที่สเปกกำหนด — ผ่าน
- **กดซ้ำระหว่างรอไม่ได้**: ทดสอบยิง click สองครั้งติดกันทันที ปุ่มถูกปิดใช้งาน (disabled) และเปลี่ยนข้อความเป็น "กำลังจัดประเภท..." ทันทีตั้งแต่ครั้งแรก ตรวจสอบ Network log ยืนยันว่ามีการยิง request ไปยัง OpenRouter เพียง 1 ครั้งเท่านั้น — ผ่าน
- **แก้ผลลัพธ์ที่ AI เลือกได้เสมอ**: ทดสอบเปลี่ยนค่าที่ AI เลือกไว้ (จาก "ลาพักร้อน" เป็น "ลากิจ" เอง) → ระบบยอมให้แก้ได้ตามปกติ ไม่ล็อกค่า — ผ่าน
- **ปุ่มบันทึกกดได้ตามปกติไม่ต้องรอ AI**: ทดสอบบันทึกใบลาสำเร็จได้ทันทีโดยไม่ต้องกดปุ่ม AI เลยในหลายรอบของการทดสอบ — ผ่าน
- **เรียกไม่สำเร็จ/รอเกิน 15 วินาทีไม่ค้าง**: ตรวจสอบโค้ด `js/new-leave-request.js` พบว่ามี `AbortController` ตั้ง timeout 15000ms และมี `try/catch/finally` ที่คืนสถานะปุ่มให้กดได้เสมอไม่ว่าผลจะเป็นอย่างไร ครอบคลุมทั้งกรณี fetch error, response ไม่ ok, และ timeout — ไม่ได้จำลองสถานการณ์เครือข่ายขัดข้องจริงบนหน้าจอในรอบทดสอบนี้ (เพราะการยิง API จริงสำเร็จทุกครั้งที่ทดสอบ) แต่ยืนยันจากพฤติกรรมของปุ่มในทุกครั้งที่ทดสอบว่าไม่เคยค้างแม้แต่ครั้งเดียว

---

## รายการบัญชี/ข้อมูลทดสอบที่ใช้ และการล้างข้อมูล

บัญชีทดสอบทั้งหมด (สมัครผ่าน `signup.html`, อีเมล `@example.com` ตามข้อกำหนดสเปกที่ห้ามใช้อีเมลจริง) ถูกลบครบทั้ง 2 ส่วนหลังทดสอบเสร็จ:

| บัญชี | อีเมล | บทบาทที่ทดสอบ | ลบ Firebase Authentication | ลบเอกสาร `users/{uid}` |
|---|---|---|---|---|
| ทดสอบ พนักงานหนึ่ง | tester.emp1.le@example.com | employee | ลบแล้ว (self-delete) | ลบแล้ว (admin) |
| ทดสอบ พนักงานสอง | tester.emp2.le@example.com | employee | ลบแล้ว (self-delete) | ลบแล้ว (admin) |
| ทดสอบ ผู้จัดการ | tester.mgr.le@example.com | manager (ปรับ role ผ่าน Console) | ลบแล้ว (self-delete) | ลบแล้ว (admin) |
| ทดสอบ ฝ่ายบุคคล | tester.hr.le@example.com | hr (ปรับ role ผ่าน Console) | ลบแล้ว (self-delete) | ลบแล้ว (admin) |

ใบลาทดสอบที่สร้างระหว่างการทดสอบทั้ง 3 ใบ ("ทดสอบระบบ - ลาพักผ่อนทะเล", "ทดสอบ - ใบลาสำหรับอนุมัติ", "ทดสอบ - ใบลาสำหรับไม่อนุมัติ") และความเห็นทดสอบทั้งหมดในนั้น ถูกลบออกจาก Firestore เรียบร้อยแล้ว (2 ใบหลังถูกตัดสินสถานะไปแล้วจึงลบผ่านปุ่มในหน้าเว็บไม่ได้ตามกฎของระบบเอง — ใช้สิทธิ์ผู้ดูแลระบบ (เทียบเท่า Firebase Console) ลบแทน) ประเภทการลาทดสอบ ("ลาทดสอบระบบ") ก็ถูกลบผ่านหน้าเว็บเรียบร้อยแล้วเช่นกัน

ตรวจสอบซ้ำแล้วว่าหลังล้างข้อมูล หน้ารายการใบลาเหลือเพียงข้อมูลตัวอย่างเดิมตามสเปกหัวข้อ 7 ครบ 5 ใบ (lr001-lr005) และประเภทการลาเหลือ 3 แบบเดิม (ลาพักร้อน/ลาป่วย/ลากิจ) ไม่มีข้อมูลทดสอบตกค้างปนอยู่

---

## การทดสอบเจาะจง 2 เคส (เพิ่มเติม)

> รอบทดสอบแยกต่างหาก ตามแผนที่ตกลงกับผู้ใช้ไว้ล่วงหน้า ใช้ Playwright MCP บนเบราว์เซอร์จริงเช่นเดิม ทดสอบหลังจากรอบตรวจสอบใหญ่ (9 US ด้านบน) เสร็จสิ้นแล้ว และไม่มีผลกระทบต่อผลของรอบก่อนหน้า

### เทสต์ 1 — ยื่นใบลาแล้วเห็นในรายการ
**สถานะรวม: ผ่านทุกขั้นตอน (8/8)**

| ขั้น | รายละเอียด | ผล |
|---|---|---|
| 1 | สมัครสมาชิกทดสอบใหม่ผ่าน `signup.html` (อีเมล `tester.case1.le@example.com`) แล้วล็อกอิน | **ผ่าน** — สมัครสำเร็จแล้วเข้าระบบอัตโนมัติทันทีในบทบาท employee (เห็นชื่อ "ทดสอบ เคสที่หนึ่ง" ที่มุมขวาบน) |
| 2 | ไปหน้า `new-leave-request.html` | **ผ่าน** — เปิดหน้าฟอร์มได้ปกติ ครบทุกช่อง |
| 3 | กรอกฟอร์มให้ครบ (หัวข้อ: "ทดสอบเคส 1 - ลากิจไปธนาคาร", เหตุผล: "ทดสอบระบบ: ต้องไปทำธุรกรรมที่ธนาคารในวันทำการ", ประเภท: ลากิจ, วันที่เริ่ม-สิ้นสุด: 2026-10-10 ถึง 2026-10-10) | **ผ่าน** — กรอกได้ครบทุกช่องตามที่ตั้งใจ |
| 4 | กดปุ่ม "บันทึก" | **ผ่าน** — กดสำเร็จ ไม่มี error |
| 5 | ยืนยันว่าเด้งกลับหน้า `leave-requests.html` อัตโนมัติ | **ผ่าน** — URL เปลี่ยนเป็น `http://localhost:3000/leave-requests.html` ทันทีหลังบันทึก |
| 6 | ยืนยันว่าใบลาที่เพิ่งยื่นโผล่ในตาราง ข้อมูลครบถูกต้อง | **ผ่าน** — แถวใหม่แสดง หัวข้อ = "ทดสอบเคส 1 - ลากิจไปธนาคาร", ประเภทการลา = "ลากิจ", สถานะ = "รอพิจารณา" (ป้ายสีเหลือง ตรวจด้วยภาพหน้าจอจริง), ผู้ขอลา = "ทดสอบ เคสที่หนึ่ง" (ชื่อไทย ไม่ใช่ uid), วันที่ลา = "2026-10-10 ถึง 2026-10-10" ครบถ้วนตรงกับที่กรอก |
| 7 | รีเฟรชหน้า (ทำโดยสั่งนำทางไปที่ URL เดิมซ้ำ ซึ่งเทียบเท่าปิด-เปิดใหม่ ทำให้แอปโหลดข้อมูลจาก Firestore ใหม่ทั้งหมด ไม่ใช่ใช้ state ค้างในหน่วยความจำ) แล้วยืนยันว่าใบลายังอยู่ | **ผ่าน** — หลัง navigate ใหม่ไปหน้า `leave-requests.html` ใบลาเดิมยังอยู่ครบทุกช่อง พิสูจน์ว่าบันทึกจริงใน Firestore ไม่ใช่แค่ state ชั่วคราวในหน้าเว็บ |
| 8 | Cleanup: ลบใบลาทดสอบ + ลบบัญชีทดสอบ | **ผ่าน** — เปิดหน้ารายละเอียด (id `s6WHAh492eQ3ZSAb0eYf`) กดปุ่ม "ลบใบลานี้" (ลบได้ปกติเพราะสถานะยังเป็นรอพิจารณา) ยืนยัน dialog แล้วใบลาหายจริง กลับสู่สถานะ "ยังไม่มีใบขอลาในระบบ" · จากนั้นลบบัญชี Firebase Authentication ด้วยการ self-delete ขณะล็อกอินอยู่ (`auth.currentUser.delete()`) และลบเอกสาร `users/TDIPevwWVogbzSmveWdTF3C6Rxj1` ผ่านสิทธิ์ผู้ดูแลระบบ (`firebase firestore:delete`) เนื่องจาก Security Rules ของโปรเจกต์ตั้งใจปิดการลบเอกสาร `users` ไว้ทุกกรณี (`allow delete: if false`) ทำให้ลบผ่านฝั่ง client ไม่ได้แม้เป็นเจ้าของบัญชีเอง |

### เทสต์ 2 — กดอนุมัติแล้วสถานะเปลี่ยน
**สถานะรวม: ผ่านทุกขั้นตอน (9/9)**

| ขั้น | รายละเอียด | ผล |
|---|---|---|
| 1 | สมัครสมาชิกทดสอบใหม่ 2 บัญชี: A (`tester.case2a.le@example.com`, uid `trR7AxsimmSaf5saMCLr4KsWrpG2`) และ B (`tester.case2b.le@example.com`, uid `BQeyNRzKCVRkHgM6BzBah39vqq83`) | **ผ่าน** — สมัครสำเร็จทั้งคู่ เริ่มต้นเป็น employee ตามค่าเริ่มต้น |
| 2 | หยุดรอผู้ดูแลโปรเจกต์ตั้ง role ให้บัญชี B เป็น "manager" ผ่าน Firebase Console | **ผ่าน** — รายงาน uid ของบัญชี B กลับไปแล้วรอการยืนยัน ผู้ดูแลโปรเจกต์ยืนยันว่าตั้งค่าเรียบร้อยก่อนดำเนินการขั้นถัดไป |
| 3 | ล็อกอินเป็นบัญชี A → ยื่นใบลาทดสอบ 1 ใบ (หัวข้อ: "ทดสอบเคส 2 - ลาพักร้อนสั้นๆ", เหตุผล: "ทดสอบระบบ: ต้องการทดสอบขั้นตอนอนุมัติของ manager", ประเภท: ลาพักร้อน, วันที่: 2026-11-02 ถึง 2026-11-03) | **ผ่าน** — บันทึกสำเร็จ ได้ id `0VuUsyhqTB7f5I2ivX2S` สถานะเริ่มต้น "รอพิจารณา" เป็นใบใหม่แยกจากข้อมูลตัวอย่าง lr001-5 โดยสมบูรณ์ (ไม่ได้แตะข้อมูลตัวอย่างจริงเลย) |
| 4 | ล็อกอินเป็นบัญชี B (manager) → เปิดหน้ารายการ ยืนยันว่าเห็นใบลาของบัญชี A | **ผ่าน** — บัญชี B เห็นใบลาของ A ("ทดสอบเคส 2 - ลาพักร้อนสั้นๆ" โดย "ทดสอบ เคสสอง เอ") ปนอยู่กับข้อมูลตัวอย่าง lr001-5 ทั้งหมดในตารางเดียวกัน |
| 5 | กดเข้าไปหน้ารายละเอียดของใบนั้น | **ผ่าน** — เปิดหน้า `leave-request-detail.html?id=0VuUsyhqTB7f5I2ivX2S` เห็นข้อมูลครบทุกช่อง |
| 6 | ยืนยันว่าเห็นปุ่ม "อนุมัติ"/"ไม่อนุมัติ" | **ผ่าน** — ปุ่มทั้งสองปรากฏให้บัญชี B (manager) เห็น เพราะใบยังอยู่สถานะ "รอพิจารณา" |
| 7 | กดปุ่ม "อนุมัติ" → ยืนยันป้ายสถานะเปลี่ยนเป็น "อนุมัติ" ทันที ปุ่มอนุมัติ/ไม่อนุมัติหายไป | **ผ่าน** — กดแล้วป้ายสถานะเปลี่ยนเป็น "อนุมัติ" (สีเขียว ตรวจด้วยภาพหน้าจอจริง) ทันทีโดยไม่ต้องรีเฟรช ปุ่มอนุมัติ/ไม่อนุมัติหายไปทั้งคู่ แสดงข้อความ "ใบนี้พิจารณาแล้ว จึงเปลี่ยนสถานะต่อไม่ได้" แทน |
| 8 | รีเฟรชหน้า/เปิดใหม่ ยืนยันว่าเปลี่ยนจริงใน Firestore + เช็คว่าฟิลด์อื่นไม่ถูกเขียนทับ | **ผ่าน** — navigate ไปหน้าเดิมใหม่ (โหลดจาก Firestore ใหม่ทั้งหมด) สถานะยังเป็น "อนุมัติ" ยืนยันด้วยการอ่านเอกสารดิบผ่าน `getDoc` ตรง ๆ ได้ผลลัพธ์ครบทุกฟิลด์: `title`="ทดสอบเคส 2 - ลาพักร้อนสั้นๆ", `reason`="ทดสอบระบบ: ต้องการทดสอบขั้นตอนอนุมัติของ manager", `leaveTypeId`="lt001", `leaveTypeName`="ลาพักร้อน", `startDate`="2026-11-02", `endDate`="2026-11-03", `requesterId`="trR7AxsimmSaf5saMCLr4KsWrpG2" (ตรงกับ uid ของบัญชี A จริง), `requesterName`="ทดสอบ เคสสอง เอ", `createdAt`="2026-09-20 12:47", `approverId`/`approverName`=ว่างเหมือนเดิม — **มีเพียง `status` เท่านั้นที่เปลี่ยนจาก "รอพิจารณา" เป็น "อนุมัติ" ฟิลด์อื่นทั้งหมดไม่ถูกเขียนทับเลย** |
| 9 | Cleanup: ลบใบลาทดสอบ (ผ่าน CLI เพราะสถานะไม่ใช่รอพิจารณาแล้ว) + ลบบัญชีทดสอบทั้ง 2 บัญชี | **ผ่าน** — รันคำสั่ง `firebase firestore:delete "leaveRequests/0VuUsyhqTB7f5I2ivX2S" --project leaveeasy-aa5a3 -r -f` สำเร็จ (ตรวจสอบผลลัพธ์ HTTP 200 จาก debug log) เนื่องจากลบผ่านปุ่มในแอปไม่ได้แล้ว (กฎ "ลบได้เฉพาะรอพิจารณา" บล็อกไว้ถูกต้องตามสเปก) · ลบบัญชี Auth ทั้งสองด้วย self-delete ขณะล็อกอินเป็นแต่ละบัญชี และลบเอกสาร `users/trR7AxsimmSaf5saMCLr4KsWrpG2` กับ `users/BQeyNRzKCVRkHgM6BzBah39vqq83` ผ่าน `firebase firestore:delete` (admin) เรียบร้อยทั้งคู่ |

**สรุปรอบทดสอบเจาะจง 2 เคสนี้:** ผ่านครบทุกขั้นตอนทั้ง 2 เทสต์ (รวม 17/17 ขั้นตอน) ไม่พบบั๊ก ข้อมูลตัวอย่างเดิม (lr001-lr005 และ leaveTypes 3 แบบ) ไม่ถูกแตะต้องหรือกระทบใด ๆ ตลอดการทดสอบ และล้างบัญชี/ข้อมูลทดสอบครบถ้วนหลังจบงานทั้งสองเทสต์

---

## เทสต์เพิ่มเติมอีก 1 เคส

### เทสต์ 3 — กรอกฟอร์มยื่นใบลาโดยเว้นช่องหัวข้อไว้ แล้วกดบันทึก
**สถานะรวม: ผ่านทุกขั้นตอน (8/8)**

| ขั้น | รายละเอียด | ผล |
|---|---|---|
| 1 | สมัครสมาชิกทดสอบใหม่ผ่าน `signup.html` (อีเมล `tester.case3.le@example.com`, ชื่อ "ทดสอบ เคสสาม") แล้วล็อกอิน | **ผ่าน** — สมัครสำเร็จแล้วเข้าระบบอัตโนมัติทันทีในบทบาท employee |
| 2 | ไปหน้า `new-leave-request.html` | **ผ่าน** — เปิดหน้าฟอร์มได้ปกติ ครบทุกช่อง |
| 3 | กรอกฟอร์มให้ครบทุกช่อง**ยกเว้นช่อง "หัวข้อ" ที่เว้นว่างไว้**: เหตุผล = "ทดสอบระบบ: ทดสอบเว้นช่องหัวข้อไว้ว่าง ต้องถูกบล็อกไม่ให้บันทึก", ประเภทการลา = ลากิจ, วันที่เริ่ม-สิ้นสุด = 2026-10-20 ถึง 2026-10-20 | **ผ่าน** — ตรวจ snapshot ของฟอร์มก่อนกดบันทึกยืนยันแล้วว่าช่อง "หัวข้อ" ว่างจริง ส่วนช่องอื่นกรอกครบทั้งหมด |
| 4 | กดปุ่ม "บันทึก" | **ผ่าน** — กดสำเร็จ ไม่มี error จากระบบ |
| 5 | ยืนยันว่า**ไม่มีการเด้งไปหน้า `leave-requests.html`** | **ผ่าน** — URL ยังคงอยู่ที่ `http://localhost:3000/new-leave-request.html` เหมือนเดิมหลังกดบันทึก ไม่มีการเปลี่ยนหน้า |
| 6 | ยืนยันว่าข้อความเตือนโผล่ขึ้นมาจริง | **ผ่าน** — ข้อความที่เห็นจริงบนหน้าจอ (ตรวจด้วยภาพหน้าจอ): **"⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก"** ตรงตามที่คาดไว้เป๊ะ ขึ้นเป็นกล่องสีชมพู-แดงด้านบนฟอร์ม ค่าที่กรอกไว้ในช่องอื่น (เหตุผล/ประเภท/วันที่) ยังอยู่ครบ ไม่หายไปไหน |
| 7 | ยืนยันว่า**ไม่มีเอกสารใหม่ถูกสร้างจริงใน Firestore** | **ผ่าน** — ตรวจ 2 ทาง: (1) เปิดหน้า `leave-requests.html` เห็นข้อความ "ยังไม่มีใบขอลาในระบบ" ยืนยันว่าไม่มีแถวใหม่โผล่มา (2) ยิง query ตรงเข้า Firestore ด้วย `getDocs(query(collection(db,'leaveRequests'), where('requesterId','==', uid)))` ได้ผลลัพธ์ `count: 0, docs: []` ยืนยันระดับฐานข้อมูลตรง ๆ ว่าไม่มีเอกสารใด ๆ ถูกสร้างขึ้นเลยสำหรับบัญชีนี้ |
| 8 | Cleanup: ลบบัญชีทดสอบ (ไม่มีใบลาให้ลบเพราะไม่ถูกสร้างขึ้นมาตั้งแต่แรก ตรงตามที่คาด) | **ผ่าน** — ลบบัญชี Firebase Authentication ด้วย self-delete ขณะล็อกอินอยู่ (`auth.currentUser.delete()`, uid `H95OqZL1vuShcwG7F8FPuNPclh33`) และลบเอกสาร `users/H95OqZL1vuShcwG7F8FPuNPclh33` ผ่านสิทธิ์ผู้ดูแลระบบ (`firebase firestore:delete`) เนื่องจาก Security Rules ปิดการลบเอกสาร `users` ไว้ทุกกรณีเช่นเดิม |

**สรุปเทสต์ 3:** ผ่านครบทุกขั้นตอน (8/8) ยืนยันว่า validation ฝั่ง client บล็อกการบันทึกได้ถูกต้องเมื่อกรอกไม่ครบ (แม้เว้นแค่ช่องเดียว) ไม่มีการเรียก Firestore เขียนข้อมูลเลยเมื่อ validation ไม่ผ่าน (ตรวจยืนยันได้จากทั้งพฤติกรรม UI และการ query ฐานข้อมูลตรง) ไม่พบบั๊ก และล้างบัญชีทดสอบครบถ้วนหลังจบงาน

---

## เทสต์ความปลอดภัยเพิ่มเติมอีก 2 เคส

> เกณฑ์ผ่านของทั้ง 2 เทสต์นี้คือ **"เข้าไม่ได้"** (ต่างจากเทสต์ก่อนหน้าที่เกณฑ์ผ่านคือ "ทำสำเร็จ") — ทดสอบทั้งฝั่งพฤติกรรม UI (client-side guard) และฝั่ง Firestore Security Rules ตรง ๆ ผ่าน browser console เพื่อยืนยันว่าไม่ใช่การป้องกันแค่ผิวเผิน

### เทสต์ 4 — ไม่ล็อกอินแล้วเปิดหน้ารายการ ต้องอ่านข้อมูลไม่ได้
**สถานะรวม: ผ่านทุกขั้นตอน (5/5)**

| ขั้น | รายละเอียด | ผล |
|---|---|---|
| 1 | ออกจากระบบให้แน่ใจว่าไม่มี session ค้าง | **ผ่าน** — กด "ออกจากระบบ" แล้วยืนยันด้วยการเปิด `index.html` ซึ่งเด้งไป `login.html` เองทันที (แสดงว่าไม่มี session ค้างอยู่จริง) |
| 2 | เปิด `http://localhost:3000/leave-requests.html` ตรง ๆ | **ผ่าน** — เปิด URL ตรง ๆ โดยไม่ผ่านลิงก์ในแอป |
| 3 | ยืนยันว่าเด้งไปหน้า `login.html` ทันที (client-side guard) | **ผ่าน** — URL เปลี่ยนเป็น `http://localhost:3000/login.html` ทันที (ตรวจด้วยภาพหน้าจอจริง) ไม่มีเนื้อหาของหน้ารายการหลุดออกมาให้เห็นแม้ชั่วขณะ |
| 4 | ยืนยันเพิ่มอีกชั้นว่า Security Rules ปฏิเสธจริง: เรียก `getDocs(collection(db,"leaveRequests"))` ตรง ๆ ผ่าน browser console โดยไม่ล็อกอิน | **ผ่าน** — ยืนยันก่อนเรียกว่า `auth.currentUser` เป็น `null` จริง (isLoggedIn: false) แล้วเรียก `getDocs` ได้ผลลัพธ์ error `code: "permission-denied"`, `message: "Missing or insufficient permissions."` ตรงตามที่คาด |
| 5 | ผ่านเมื่อทั้งสองข้อ (UI เด้ง + Firestore ปฏิเสธ) เป็นจริง | **ผ่าน** — ทั้งสองเงื่อนไขเป็นจริงพร้อมกัน (ขั้นที่ 3 และ 4 ข้างต้น) |

### เทสต์ 5 — ล็อกอินด้วยบัญชีที่สอง แล้วเปิดใบลาของบัญชีแรก ต้องเปิดไม่ได้
**สถานะรวม: ผ่านทุกขั้นตอน (6/6)**

| ขั้น | รายละเอียด | ผล |
|---|---|---|
| 1 | สมัครบัญชีทดสอบ A (`tester.case5a.le@example.com`, uid `PpohLyAiSJZaleKduo6CjTPSnG43`) → ล็อกอิน → ยื่นใบลาทดสอบ 1 ใบ ("ทดสอบเคส 5 - ใบลาของบัญชี A") → จด id ของใบลานั้นไว้ (`x3mIrqK9ZB97HDQ2dVcM`) → ออกจากระบบ | **ผ่าน** — สมัครและยื่นใบลาสำเร็จ ได้สถานะ "รอพิจารณา" ปกติ จดบันทึก id ก่อนออกจากระบบเรียบร้อย |
| 2 | สมัครบัญชีทดสอบ B (`tester.case5b.le@example.com`, uid `zSQOnIE0a5hlAuky59Wk7682Ta42`, role employee เหมือนกัน) → ล็อกอิน | **ผ่าน** — สมัครสำเร็จ เข้าระบบอัตโนมัติเป็น employee |
| 3 | เปิด `leave-request-detail.html?id=x3mIrqK9ZB97HDQ2dVcM` (id ของใบลา A) ตรง ๆ ผ่าน URL ขณะล็อกอินเป็น B | **ผ่าน** — เปิด URL ตรง ๆ ตามที่ตั้งใจ |
| 4 | ยืนยันว่าเปิดไม่ได้จริง (ขึ้นข้อความ "ไม่พบใบขอลาที่ต้องการ") | **ผ่าน** — ข้อความที่เห็นจริงบนหน้าจอ (ตรวจด้วยภาพหน้าจอจริง): **"ไม่พบใบขอลาที่ต้องการ — อาจถูกลบไปแล้ว หรือลิงก์ไม่ถูกต้อง"** ไม่มีข้อมูลของใบลา A รั่วออกมาให้เห็นเลยแม้แต่ช่องเดียว |
| 5 | ยืนยันเพิ่มอีกชั้นผ่าน console ว่า `getDoc` ของใบลานั้นภายใต้บัญชี B ได้ `permission-denied` จริง (ไม่ใช่แค่ id ไม่ตรง) | **ผ่าน** — เรียก `getDoc(doc(db,'leaveRequests','x3mIrqK9ZB97HDQ2dVcM'))` ขณะ `auth.currentUser.uid` ยืนยันแล้วว่าเป็น `zSQOnIE0a5hlAuky59Wk7682Ta42` (บัญชี B จริง ไม่ใช่ B สลับกับใคร) ได้ผลลัพธ์ error `code: "permission-denied"`, `message: "Missing or insufficient permissions."` — ยืนยันว่า id ถูกต้องแต่ถูกกฎเฝ้าข้อมูลปฏิเสธจริง ไม่ใช่กรณี "ใบลาไม่มีอยู่จริง" |
| 6 | Cleanup: ลบใบลาทดสอบของ A + ลบบัญชีทดสอบทั้ง A และ B | **ผ่าน** — ล็อกอินกลับเป็นบัญชี A แล้วกดปุ่ม "ลบใบลานี้" ในหน้ารายละเอียด (ลบผ่านแอปได้ปกติเพราะสถานะยังเป็นรอพิจารณา) ยืนยัน dialog แล้วใบลาหายจริง กลับสู่ "ยังไม่มีใบขอลาในระบบ" · ลบบัญชี Firebase Authentication ทั้ง A และ B ด้วย self-delete ขณะล็อกอินเป็นแต่ละบัญชี และลบเอกสาร `users/PpohLyAiSJZaleKduo6CjTPSnG43` กับ `users/zSQOnIE0a5hlAuky59Wk7682Ta42` ผ่านสิทธิ์ผู้ดูแลระบบ (`firebase firestore:delete`) เนื่องจาก Security Rules ปิดการลบเอกสาร `users` ไว้ทุกกรณีเช่นเดิม |

**สรุปเทสต์ความปลอดภัย 2 เคสนี้:** ผ่านครบทุกขั้นตอนทั้งคู่ (รวม 11/11 ขั้นตอน) ยืนยันว่าการป้องกันข้อมูลทำงานถูกต้องทั้ง 2 ชั้น — ชั้น UI (client-side guard ที่เด้งไป `login.html` และแสดง "ไม่พบใบขอลาที่ต้องการ") และชั้น Firestore Security Rules ที่ปฏิเสธจริงในระดับเซิร์ฟเวอร์ (`permission-denied`) ไม่ใช่การป้องกันแค่ผิวเผินที่ซ่อนปุ่มอย่างเดียว ไม่พบบั๊ก และล้างบัญชี/ข้อมูลทดสอบครบถ้วนหลังจบงาน
